# Production environment configuration for livefront-react-app
# This file is loaded when building for production
# Values here override .env but are overridden by .env.local if it exists

# Add a production-specific API key here or use a CI/CD system to inject it
# VITE_NASA_API_KEY=YOUR_PRODUCTION_API_KEY

# Production-specific settings
VITE_ENV=production
VITE_API_TIMEOUT=5000
VITE_ENABLE_LOGGING=false
VITE_DEBUG_MODE=false

